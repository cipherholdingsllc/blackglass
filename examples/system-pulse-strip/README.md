# System Pulse Strip

Purpose: a compact rail for semantic status snapshots.

Why it exists: state should stay visible without becoming metrics noise.

Visual state: active, verified, waiting, blocked, and deferred.

Accessibility note: every pulse includes a label and does not rely on color alone.

Do not use when: the strip would imply fake live activity.

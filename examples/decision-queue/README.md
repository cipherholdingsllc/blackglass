# Decision Queue

Purpose: a grouped lane for required, next, and optional choices.

Why it exists: prioritization should be visible without creating panic.

Visual state: one required decision, two next choices, and one optional note.

Accessibility note: priority appears in text, lane title, and position.

Do not use when: the viewer cannot act on the choices.
